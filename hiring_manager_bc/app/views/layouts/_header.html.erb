<header class="navbar navbar-fixed-top navbar-inverse">
    <div class="container">
            <%= link_to "Hiring manager", root_path, id: "logo" %>
            <% if logged_in? %>
            
            <nav>
                <ul class="nav navbar-nav navbar-right nav-item">
                    <li><%= link_to "Application",   applications_path  %></li>
                    <li><%= link_to "Feedback",   feedbacks_path %></li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <%= current_user.name %><b class="caret"></b>
                        </a>
                        <ul class="dropdown-menu">
                            <li><%= link_to "Profile", applications_path %></li>
                        <li class="divider"></li>
                        <li>
                            <%= link_to "Log out", logout_path, method: :delete %>
                        </li>
                        </ul>
                    </li>
                </ul>
            </nav>
        <% end %>
    </div>
</header>